{
    "size":{{size}}{{^size}}10{{/size}},
    {{#from}}"from":{{from}},{{/from}}
    "aggregations": {
        "brand": {
            "terms": {
                "field": "brand"
            }
        }
    },
    "query": {
        "prefix": {
            "categoriesMap": "{{categoriesMap}}"
        }
    }
}